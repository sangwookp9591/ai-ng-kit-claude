{
  "name": "@aing/browse",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Persistent headless browser daemon for ai-ng Claude Code plugin",
  "bin": {
    "aing-browse": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/start-server.js",
    "postinstall": "npx playwright install chromium --with-deps 2>/dev/null || true"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
